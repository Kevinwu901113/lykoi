# /home/lykoi/secrets/telegram-cordis.env · 样例（占位符，不含任何真值）
#
# 落址与权限（docs/deploy.md §7）：
#   install -o root -g root -m 600 /dev/null /home/lykoi/secrets/telegram-cordis.env
#   sudoedit /home/lykoi/secrets/telegram-cordis.env
#
# 这是**唯一**该进 unit env 面的 LYKOI_*（m4_handoff.md 前置 #11）。
# 完整性门对它的态度：GK-6 钉面的 secret 类 —— 只看设没设、空不空，
# **值永不比对、永不落日志**（surface.ts 的 ENV_PINS 末条 + verify.ts 的
# checkEnvPins `case 'secret'`）。
#
# 传输层同样零明文：token 走 env 引用（装配面的 tokenEnv），只活在
# BotApiTransport 的私有字段里；无 token 即拒起；请求 URL 里含 token，所以
# 传输层绝不让任何原始异常逃出去（packages/lykoi-adapter-telegram/src/http.ts）。
#
# 本文件**永不入库**。仓库里只有这份样例，值只活在服务器上。
#
# 注意这里**没有** LYKOI_TELEGRAM_PROXY：它是 GK-6 的 unset 类，
# 生产必须未设（设了就是一条外泄通道）。出站代理走签名过的
# profile/cordis.prod.yml 的 telegram-transport.config.proxy。

LYKOI_TELEGRAM_BOT_TOKEN=<BotFather 发给你的 bot token>
