# WO-U3S 复核 · 周期合一切换单

- 复核人：治理 Agent（主窗口）
- 复核日期：2026-08-22 凌晨
- 级别：必要检测级（owner 提速指令延续）
- 执行情况：见 §4 执行考古——两波实为并行，第 1 波跑完全程
- **结论：PASS（追认项 2 条，见 §5）**

## 1. 合规核

- 分支 `wo/u3s` 尖 `55921d33`，基 `7b00ae5e`，工作树干净。
- **足迹独立核查**：diff 全集 = 3 个源文件（`cognition/conversation.py`、
  `conversation_cycle.py`、`llm_router.py`，恰为 manifest 三条改哈希）+ 5 个测试
  文件 + `tests/conftest.py` + `guardian/manifest.sha256`。**封存区零 diff**
  （kernel/core/mind/resources/guardian `*.py` 以 `git diff --stat` 独立确认为空）。
- 既有测试改动两处均核过：`test_approval_delivery.py` +4 行（给切换态补同款
  安静假回合，不弱化断言）；`test_u3_conversation_cycle.py` = 红测试交接
  （按其 docstring 预定方式退役，继任两条在 `test_u3s_switch.py`，已定位）。
- forbidden 自查五项与报告一致；secrets 零入账口径有用例扫。

## 2. 独立验证

- **manifest 独立重算**：110 条逐条 sha256 复算，**0 差异**；唯一不可读 =
  `/home/lykoi/state/approval_rules.json`（owner 域，沿签既有哈希，与报告口径
  一致）。
- **合并预演（治理侧模拟）**：一次性克隆里做 `32238013 × wo/u3s` 真实
  `merge --no-ff`——**零冲突**；合成 manifest = 112 条，u3s 三条新哈希与 GW 两条
  新增并存，三个 cognition 文件对合成树逐一复算相符。粘贴稿因此可走干净合并，
  conflict fallback 仅留防意外。
- **关键套件独立复跑**（八文件：u3s 三新套件 + E1/E2 全套 + conversation_cycle +
  governance_invariants）：{{RERUN}}
- 新增测试计数对账：31+8+14 = 53，与报告"新增 53"吻合；全量 collected
  2169 = 基线 2117+53−1（退役红测试）。

## 3. 白皮书/判据对照要点

- 判据①"唯一读者"：`Conversation._switch_on`，全 src 恰好定义+一读者，静态用例
  钉住——37.6"她不能自己批准自己"与切换键治理面归属不受影响（env 由 root drop-in
  控制，代码无写路径）。
- 判据②影子停用 = 成本回到一轮一调用；`route=main` 实验组身份延续，U2 读数可比。
- 判据⑧审批送达在切换态**复用既有接点**（无新接点），FIX-APPROVAL-DELIVERY 的
  修复未随转录机让位蒸发——DP2 加固目的达成。
- 失败方向 = 沉默（不变量 3），不回落旧路径——与 8-19 影子首夜的安全结论同向。

## 4. 执行考古（重要，含治理侧自我更正）

- **第 1 波从未死**。20:18 起跑，23:33 时"进程组死亡"是治理侧误诊（`ps` 输出被
  `head -8` 截断漏看）。它实际跑完全程：五判据 commit（20:46–21:17）→ 2h 全邻接
  分块 → 复跑 → **03:08:01 EXIT=0 + 295 行报告**（report.md 即它的产物）。
- **第 2 波系误派**（23:57）。其产物 4 commit：`4a604025` manifest 重签（与第 1 波
  本地重算逐字节相同，第 1 波报告已核）+ 三条测试假红修正（`8d30930e`/`84a94760`/
  `21bbc0d4`，方向与第 1 波观察一致，第 1 波折进复跑）。第 4 条假红（时钟）由
  第 1 波自己补（`55921d33`）。第 2 波剩余任务与第 1 波交付重复，03:1x 由治理侧
  查明后杀掉止损（governance-ops 已记）。
- 报告 §2"四个不是我打的 commit"即第 2 波产物——**来路已澄清，内容已双向核**
  （第 1 波逐个核对 + 本复核套件复跑覆盖三条修正所在文件）。
- **教训候选（入 HANDOFF）**：①判执行器死活必须 `pgrep -f` / 全量 `ps`，禁用
  截断输出下结论；②误诊重派的后果是同树双执行器竞写——本次侥幸无冲突
  （第 2 波 manifest 与第 1 波逐字节同、三修正方向一致），不可依赖侥幸。

## 5. 追认项（呈 Kevin）

1. **判据①②③⑥ 同 commit**（`a20ad743`）：铁律"每判据一 commit"偏离。执行方
   理由成立（同一段方法的接线，拆开会产生"信封接管但念头静默丢失"的撒谎中间态），
   建议追认。
2. **全量口径**：稳态 2155/8/6，其中 8 = 7 条 `test_core_v1_shadow` 环境性
   （教训 38 形态；基分支同负载 12 条，比尖端更红，执行方做了决定性对照）+ 1 条
   p0 claude 身份假失败。**本单新增失败 0。**双执行器负载下的数字浮动属环境，
   建议按"基线对照定性"口径追认。

## 6. 交治理侧/合并包的事项

- 风险 1（报告 §4.1）：契约失败=沉默，json 样本仅 1。**B 段后盯
  `u3_cycle_failed`，非零删 drop-in 止损**——已写进粘贴稿第 6 步。
- 风险 2：silence 轮 + 待批横幅可能单独成一条消息（`surface/app.py` 既有规则，
  本单依判据④未动）→ **入 U4 清单**。
- 信封契约 ~1.5k 字符不进上下文预算（沿用影子期口径）→ 记档备查。
- 部署面：仅 `lykoi-server` 需 drop-in + 重启（telegram/autonomy 零 diff 不必），
  与粘贴稿一致；A/B 两段验证口径照报告 §3。
