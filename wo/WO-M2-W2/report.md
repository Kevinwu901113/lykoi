# WO-M2-W2 · 快照与决策 · 执行报告（治理侧存档）

- 执行：Mac 本地 Agent（撞额度一次续跑收口）；产物 commit：lykoi-cordis `0d267b2`（基 9b1591b）
- 复核：独立复跑 222（213+9 devstate skip，注入后 64/64）全绿 + tsc 净 + G-2 sha 钉死核验
  （测试断言新 sha + 源码零 next_wake 残留）——**PASS**

## 交付

1. **lykoi-snapshot**：maintain/read 劈分（SA-33..36 四写顺序逐字/moment 共用）+ floor
   （SA-173/174 全套）+ read 九项键序与裁剪常量逐字（SA-37..44）+ 环境块（节律采样三纯
   函数/探索断粮/**G-6 落地**=floor(20×multiplier)−used）+ SA-44 总压力钳 +
   renderRestartNotice 逐字 + **pyRound**（CPython round 忠实对应，20 组 python3 golden）。
2. **lykoi-decide**：统一契约（SA-01..32：KINDS 渲染锚/14 字段/as_dict drop-list/三分支
   互斥/棘轮三条件/11 步解析链/demote 分野/inner 全套/四词汇表参数化边界）；
   **G-2**：DECIDE_SYSTEM_PROMPT 移除发言权字段行，旧 sha `a495848d…a636e`(1634) →
   新 sha `d54726e3…3e74c`(1601)，测试钉死，DA-04 随之消失；**G-7**：buildMessages 五段+
   器官块注入自主侧（内核装配共享函数=两侧逐字节同源）；persona 内核九段 fixture sha
   全等 `1f5960b7…bec71`+投影三形状+影子门测试钉死（focus 不在白名单）；organs 渲染器
   四禁落实（channel_key 在类型上不存在）。
3. **rw 补齐**：快照所需读写 API 全套（含 markDimmingDormant 7/21 天、createConcern
   cap12）+ regulation 唯一增量 QUESTION_OVERDUE_HOURS=48。

## 定案与销账

- **W1 TODO 5/6/10 对拍销账**：getAutonomyRuns 排序键逐字一致；decision 序列化=保序
  as_dict+JSON.stringify（跨语言表示差读侧双向兼容）；懒衰减三例 CPython IEEE 位级 golden 全等。
- **新增 G-11**（W2 上报的 rest note 死引用张力，治理定案）：改文案 `load {delta};下一拍
  由心脏节律决定`，W3 落地，入追认清单。

## golden 红测：decide 12 路 + inner 8 路 + snapshot 8 路 + memory 4 路（见 lykoi-cordis
测试套件；零写断言含对照组 SA-48）。

## 新增 TODO（6 条）：G-11 落地(W3)/快照 deps 四接口位接线(W3/W5)/persona TOML 加载与
organs 真实来源(W5)/logEvent 统一接 audit(W3)/双夹具 DDL 同改纪律(W3 可并)/跨语言等价档留痕。

## 纪律核验

零偏离（两处蓝图未定处最小方案留痕）；golden mtime 前后全等；行内容零输出；
lockfile 仅 +22 行 workspace link；报告一次性输出。
