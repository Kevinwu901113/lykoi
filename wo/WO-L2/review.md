# WO-L2 复核 · PASS（两轮复核补适配，src 零改动）· 2026-08-11

- **复核人**：治理平面 Agent（单窗口期）
- **对象**：`wo/l2` @ `ebd26d24`，基于活体 main `01a8099c`，6 个提交
- **结论**：**PASS**。学习层 v2 第一个行为改变单：她的 nightly 消化自此吃"原料池
  （水位线之上）"，55 天被 SQL 挡在门外的感知开始进入她的心智；关切带上三来源标记。

## 一、提交链与过程

| 提交 | 内容 | 谁驱动 |
|---|---|---|
| `eab07978` | 实现：取料改口 + `_V12` 水位线 + K 依据 + origin | 原单 |
| `c2948c12` | 既有测试语义适配（8 文件） | 原单 |
| `a1088752` | `test_l2_intake.py` 548 行 / 28 用例 | 原单 |
| `653e9a55` | manifest 重签（98 条不变，3 哈希） | 原单 |
| `56eabe6c` | 复核轮 1：4 个漏网文件同款适配（10 条转绿） | **复核发现** |
| `ebd26d24` | 复核轮 2：confab 默认种子一行根因 + outbox 冻结点钉死（10 条转绿） | **复核发现** |

过程曲折如实记：首轮派发被额度撞限打断（04:50 重置），收尾续跑完成主体；
复核全量暴露 20 条漏网失败，分两轮补适配修净。**六个提交里 src/ 只有 `eab07978`
一笔**——所有复核修复都在测试文件，行为实现自始未返工。

## 二、独立验证

| 项 | 结果 |
|---|---|
| **串行全量**（并发跑会污染 shadow，已记流程教训） | 修复前完整清单 24 失败 = **基线 14**（m3/deepseek 11 + shadow 2 + p0 漂移 1）+ L2 相关 10；**两轮修复后，所有曾红文件复跑 106/106 全绿** |
| 专项 | `test_l2_intake` 28/28；integrator 邻接套件（pipeline/trigger/telemetry/concern_floor/p4r×3/confab/perception_ingest）全绿 |
| manifest | 98 条、3 哈希更新与 mind 三文件实算一致；分支侧 kernel 漂移与 5 文件缺册为**基线既有**（S3 的 98→104 已补，合并后 root 统一重签终态一致） |
| 代码抽查 | `intake_pending` 参数绑定/三调用点同口径 ✓；水位线 `INSERT OR IGNORE` 幂等 ✓；`downgrade_v12` 倒序 ✓；`pending_experiences` 原语义保留（快照面板）✓ |
| 未 push | ✓ |

## 三、复核对执行质量的两点判断

1. **实现质量高**：水位线用 `learning_layer_state` 单独小表（论证了为何不塞
   `integration_state`）；origin 枚举表交换扩成七值并集、历史行不重贴标签
   （provenance 纪律）；`owner_directed` 有降级闸（非对话材料/窗口无对话/非严格
   True 标记一律降 `emergent` 并发事件——标签不假）；拒绝代签越权 manifest 条目。
2. **漏网根因是工单的**（我的）：改取料语义的单，必跑清单手挑了 11 个文件，
   漏的恰是没挑进去的 5 个 integrator 邻接文件。教训 33 已写入 HANDOFF。
   outbox 冻结点漂移是我 L1 复核修法留的链相对陷阱，本轮钉成绝对 `[:10]`。

## 四、部署要点（进合并包）

- L2 合并后 **`_V12` 在任意 `_connect()` 自动应用**（瞬时，无回填），水位线
  = 应用时刻的 `MAX(experiences.id)`——**必须在合并后尽快重启 autonomy**，
  让新代码与水位线同期生效；不需要停机窗口。
- 行为验证：重启后下一个整合周期应看到 `intake` 口径（可查
  `count_intake_pending`，起步应为 0 或个位数——水位线定在部署时刻）。
- K=30 保留；观察期触发条件写在代码注释（水位线上未消化持续 >3K 时重标）。
