# 合并包 5 · 2026-08-12 · 对话审批四处补漏【草稿·待复核终审后定稿】

> 状态:**草稿**。执行 Agent 在写,我终审(全量串行 + manifest 重算 + 复核者补丁:
> 仓库正本 lykoi-telegram.service 补 llm.env 行)后定稿。

修复内容:①执行成功主动回报(引用批准消息,S1A 豁免);②老式 POST 横幅退役
(对话撞门改走 S3 问询,不重复播报);③「执行/不要」字面快通道(零 LLM 依赖);
④前置过滤词表收自然应答。附:仓库正本 lykoi-telegram.service 补
`EnvironmentFile=/home/lykoi/secrets/llm.env`(你的 drop-in 转正,防重部署丢失)。

无迁移。合并 → 属主还原(kernel 两文件保持 root)→ 重签(107 条不变,哈希更新)
→ GATE_OK → 测试 → 重启五服务。

## 合并后的实弹验收(你在 Telegram 上走一遍,2 分钟)

1. 对她说:"帮我看下服务器现在几点"(或任何会触发 terminal.exec 的小事);
2. 期望:**一条**不带 POST 端点的问询(旧横幅绝迹);
3. 直接回「执行」(可以不引用——快通道;引用也行);
4. 期望:**她立刻引用你的批准回执行结果**,不需要你再发任何消息。
5. 换个说法再试一次"批准/同意"——应当也被认成同意(词表+LLM 判读)。

(命令区待复核终审后填入,结构同包 4:0 树检查 → A fetch+tag+merge →
B 属主+重签 → C 测试 → D 重启五服务 + 验收读数。)
