# WO-CA-BASELINE-1 · C-A 认知核基线审查 · 前半(调度侧/自主循环,只读)

**背景地位**:Cordis 接轨方案 C 线入口门 C-A 的**前半**。C-A 原定 U3/U4 收官
后整审;Kevin 2026-08-19 指令提速,治理侧拆分:与 U3/U4 无关的调度侧/自主
循环/写通道/费用画像**现在审**,对话路径审查(后半)仍留 U4 后——转录机即将
退役,现在审它是白审。本单产物直接喂:C-B 心脏设计、9.4 尾注重评附文、
白皮书 36 章正文素材。

你是**审查 Agent,只读**:零代码修改、零 commit(工作树必须保持干净),
**stdout 即报告本体**,宁长勿略。在 `~/lykoi-work-ca` 工作(分支
`wo/ca-baseline` 尖 `7b00ae5e` = 活体等价)。引用一律带代码行;区分
**[事实]**(代码/配置直接可证)/ **[推断]**(有依据的判断)/ **[建议]**;
不臆测,查不到就写查不到。

参考材料(同目录):`whitepaper_v1.2.md`(9.4/10.3/12.3/22.2/31 章为本单
密切相关章)、`cordis_integration_plan_2026-08-18.md`(§1 对账表)、
`cognition_architecture_exploration_2026-08-17.md`(心脏—大脑—器官语义)。

## 审查范围(七节,每节一章报告)

① **自主循环全景**:`cognition/autonomous.py` 与 `mind/decide.py` 自主情境的
   完整控制流——定时形态(周期常量/抖动/退避)、每周期做什么、`evaluate_message`
   /`apply_inner` 的自主侧消费、动作派发路径。画出文字版时序。对照白皮书
   10.3 与 Cordis 双时钟:**缺口清单**(显式心脏调度件、调度侧显著性唤醒、
   tick 积压合并各自差多远,现状雏形在哪几行)。
② **唤醒路径清点**:今天有哪些事件能触发一次认知周期(消息即时唤醒/定时/
   focus 周期/其他),每条路径的入口代码行、节流机制、互相关系;有没有唤醒
   风暴的结构风险(如 CORE_ATTENTION_DECISION_CYCLE 每 2s 空转刷屏这个已知
   遗留,给出它的确切来源与去害建议)。
③ **状态写通道清点(9.4 符合性)**:memory.db(及其他持久 state)的
   **表 × 写者矩阵**——每张表谁写、经什么函数、什么进程;单写者原则逐条核
   (已知的受治理写者白名单如 focus.py 列明);**并发形态若引入(快照并行推演
   +串行仲裁)哪些通道破单写者**,逐表标注。
④ **费用画像(口径,不读数)**:llm_call 计量的产生点、route/origin 标签全集、
   四数字段语义;若心脏引入更高唤醒频率,费用敞口在哪几个调用点;12.3/22.2
   预算硬策略今天的实际执行机制(代码里在哪强制)。
⑤ **9.4 尾注重评素材**:按 Cordis 并行推演+串行拍板形态(快照并行、仲裁器
   唯一写者、议程项租约),给出:需要租约覆盖的议程项类型清单、快照一致性
   边界(哪些读路径可以吃旧快照、哪些不行)、与③矩阵的冲突点全列。此节是
   素材与分析,**结论留治理侧与 Kevin**。
⑥ **C-B 演化路径建议**:从现 autonomous 定时循环到显式 heartbeat 调度件的
   **最小步序**(影子形态优先:心跳件与旧定时并行、只置位不消费→对比唤醒
   次数/费用/时延→切换),每步的改动面预估与判据素材。
⑦ **风险与重构资产清点**:调度侧的死代码/孤儿模块(如 percept_buffer 类)、
   耦合热点、动它之前必须先补的测试。

## forbidden

零代码修改、零 commit、零新文件(报告走 stdout);不审对话路径/conversation.py
/telegram(C-A 后半领地);不读 secrets;不碰运行中的服务与 state 写入
(memory.db 等 0600 读不到就写读不到,不 sudo);不给结论性拍板(9.4 重评
结论是治理侧+Kevin 的事);凡与材料冲突的发现,如实记录标注。
