# WO-CA-BASELINE-1 复核 · C-A 前半（调度侧/自主循环）

- 复核人：治理 Agent（主窗口）
- 复核日期：2026-08-21
- 执行情况：opus 单次 EXIT=0（2026-08-19 20:37–20:57，20 分钟），报告 64KB
- **结论：PASS**

## 1. 合规核

- **零写入自证独立验证通过**：2026-08-21 复核时 `git -C ~/lykoi-work-ca status --porcelain` 空、HEAD `7b00ae5e`，与报告尾部自证一致。只读单守住了只读。
- **forbidden 全守**：未审对话路径（只在必要处引用入口行并声明）、未读活体数据、未碰运行中服务。
- **材料缺失的处理方式正确**：三份参考材料（白皮书 v1.2/Cordis 接轨方案/认知架构探索）执行方读不到（根因见 §4），报告选择"不复述、不臆测、开口留给治理侧"——纪律执行到位，全文未发现一处冒充原文的转述。

## 2. 事实抽查（10/10 全对）

| # | 主张 | 核法 | 结果 |
|---|---|---|---|
| 1 | 调度常量表（TICK 5s、REST 5/30/360、HOURLY_CAP） | sed 原文 | ✓ 逐字对上 |
| 2 | `_due()` 对空值与解析失败 fail-open | `autonomous.py:205-211` 原文 | ✓ `except ValueError: return True` |
| 3 | `counts["action"]` 只在 explore/initiate_chat/queue_notification 三处 +1 | grep + 分支上下文 | ✓ 三处，contemplate/rest 等不计 |
| 4 | `snapshot.assemble()` 读前四写 | `snapshot.py:322-336` 原文 | ✓ 且注释自认有意 |
| 5 | `CORE_ATTENTION_DECISION_CYCLE` 每 2s 无条件 print | `attention_decision.py:17` + `runtime.py:1488-1496` | ✓ |
| 6 | `llm_call` usage 唯一产生点、不带 origin 归因 | 全树 grep | ✓（focus 的 llm_calls 是内部计数，非 usage 事件） |
| 7 | `percept_buffer` 完全孤儿 | grep | ✓ 仅模块+conftest+自身测试 |
| 8 | autonomous 的 `run_forever` 测试覆盖为零 | grep tests/ | ✓ 全部命中是 telegram 的同名函数 |
| 9 | `mark_rule_suggestion_asked` 是现成租约（rowcount CAS） | `store.py:2460-2465` | ✓ `claimed = cursor.rowcount == 1` |
| 10 | `interactive_activity.json` 唯一不带 file_lock 的跨进程 JSON | grep interactive_lock.py | ✓ 仅 write_json_atomic |

## 3. 白皮书对照（补齐报告留下的开口，正本 v1.2 2026-08-18）

- **9.4 单写者原则**：条款要求"经历整合与状态更新必须排队通过唯一写入者"。报告 §3.4 实证：经验流五张表今天是**三进程写者靠 DB 层 `BEGIN IMMEDIATE` 短事务串行**——"串行写入通道"成立，"唯一写入者"作为实体不存在。这不是违规（今天的写确实排队），但 37.5 的仲裁器形态正是把"DB 层串行"升格为"实体层唯一写者"。**报告 §5 的 L1-L10/C1-C12 两张表即 9.4 尾注要求的重评素材，本单交付有效。**
- **10.3 当前实现**：白皮书描述的 snapshot→candidate→LLM→reflow→rest 闭环与报告 §1.3 时序逐项吻合，无冲突。
- **37.2 双时钟**："现状：自主循环（10.3）与消息即时唤醒是本条雏形"——报告 §2.2 把这句话磨尖了：**消息"即时唤醒"的是对话进程里的 P6 周期，对自主循环只有 120s 压制，两个时钟今天互不相通**。白皮书表述不算错（消息确实即时触发一次认知），但 v1.3 候选注记：应写明"两钟分属两进程、无互唤通路，接通即 C-B 主要工程"。
- **12.3 自主预算 + 22.2 硬性策略**：两处都把 **Token/实际费用**列为必须受约束/硬顶项。报告 §4.6 实证：**代码里 token/费用维度的强制机制为零**（只有行动/打扰配额与单次 max_tokens）。**确认为规范-实现缺口**，与 Cordis 拍板"费用不管仅限 MVP"一致——此缺口必须作为 C-B 心脏工单的硬判据带入（心跳提频与费用闸必须同单落地，见 §5 敞口分析：提频不只线性放大决策调用，层 1/层 2 因闸读 wake 计数也随频率翻倍）。
- **31.3 必要产物**：本单交付调度侧的架构图（文字时序）、写通道矩阵、风险清单、死代码/资产清点、可保留/待重构清单素材——前半份额达成，对话路径份额按计划留 U4 后。

## 4. 材料缺失事故（责任在治理侧派发）

工单参考材料投放在 `~/wo/WO-CA-BASELINE-1/`，但 bin-dispatch 只把 order.md 经 stdin 喂给执行方，执行会话读不到 `~/wo`（`ls /home/claude` 被会话权限拦）。**已修**：bin-dispatch.sh 2026-08-21 加 `--add-dir ~/wo/$WO`（HANDOFF 教训 40）。本报告的白皮书对照开口由复核侧补齐（§3），**不退单**——报告在缺材料条件下的处理方式恰是我们想要的执行方行为。

## 5. 采纳的产出与新立总账项

**采纳为 C-B 设计输入**（正本即本报告）：缺口 1-3 表、⑥最小步序（步 0 劈 `assemble()` 纯重构可先行；步 1-3 心跳影子与步 4 推演切分**不捆一单**）、⑤租约/冲突全表、⑦补测清单。

**新立缺陷/风险（入总账）**：
- **R-CA-1（唤醒风暴，低概率高放大）**：`_due` 脏值 fail-open + rest 类动作不计小时 cap → 脏时间戳可致每 5s 一次 LLM 调用无限烧。修法便宜：fail-closed+telemetry、最小拍间隔地板。→ 小单候选 **WO-FIX-AUTONOMY-FLOOR**。
- **R-CA-2（已知遗留精确化）**：core 2s 空转刷屏根因三行定位，推荐报告方案 1（与同文件另两个 tick 同款条件闸，同步改 `tests/test_core_v1_runtime.py:1325` 断言）；方案 2 有安全语义副作用（policy 哈希校验频率）明确不顺带做。→ 遗留销账候选。
- **R-CA-3（C-B 阻塞级测试缺口）**：`run_forever` 骨架零测试、`_due` 边界零测试、"推演零写入"断言（今天必失败=步 0 必要性证据）。→ 必须写进 C-B 首单判据。

**留给 Kevin 的设计问题**（C-B 设计阶段拍板，勿在实现期被动碰到）：
1. 心跳件接管后，模型还保不保留"我想 N 分钟后再醒"的发言权（H5，牵动 DECIDE prompt 契约）；
2. 显著性输入二选一：salience_shadow（同进程、便宜）vs Core attention_decisions（跨进程、epoch 锁）——报告建议先 salience_shadow 走通形态，我附议；
3. 层 1/层 2 节律绑 wake 计数不绑墙钟——提频时整合/深挖随之提频（费用最大敞口），是保持绑定还是改锚墙钟。

## 6. 决审

高质量只读审查：材料缺失下的纪律、三命名空间辨析、"L7 租约已存在""C7 正面样本"这类把现成资产找出来的眼力、以及"步 1-3 与步 4 正交勿捆"的工程判断，都超出工单字面要求。**PASS，报告全文采纳为 C-B 设计正本输入。**
