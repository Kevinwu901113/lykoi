# 合成测试实例包的记忆种子（WO-E4-2）—— seedPersona 在实例出生时写进 insights 的
# 后天种子。形态：[seeds] 表，键 = insight category，值 = 该类种子内容（字符串数组）。
# 文件缺失 = 零种子；损坏 = 出生阶段抛错。内容是合成值，不描述任何真实个体。

[seeds]
preference = ["合成种子：Owner 偏好用中文交流，技术术语用英文（测试实例包）"]
