# Lykoi Cordis · **dev** profile（插件树；直排 entries；M1 不用 bundle 分层）
# 顶层必须是数组（WO-M0-DSH-STUDY §2.1）。路径类配置相对进程 cwd（从仓库根 npm start）。
#
# **dev 与 prod 两套分明**（M3-W4 / M2 遗留 #10）：
#   - 本文件 = dev：开箱能跑、零真网、零 root 供给、写 `var/` 下的本地副本。
#     M3-W4 一个字节都没动它的行为 —— 生产条目全部落在另一份里。
#   - `profile/cordis.prod.yml` = prod：规范 state 路径、audit sink 落
#     /var/log、GK-8 开关（默认关）、D-01 超时与 vision 路由的 M4 值位。
#     每一条 `disabled` 都标了需要哪一样 root 供给才能翻开。
#
# 两份都在 GK-13 的 **root 属主域**（完整性门 hash 钉住）：装配面说 audit sink
# 落在哪、哪些器官启用，那是治理决定，不是随手改的配置。改完要 root 重签：
#   sudo node packages/lykoi-gate/src/cli.ts --write-manifest
- id: timer
  name: '@deepseek-ai/cordis-plugin-timer'
- id: audit
  name: lykoi-audit
  config:
    path: var/audit.jsonl
- id: budget
  name: lykoi-budget
  config:
    ledgerPath: var/budget.json
    # dev 硬顶：mock 路由 50 tokens/UTC 日（两次 34-token 调用后第三次必拒 → 红测），
    # 总量 1000。生产硬顶 M3 治理侧定。
    dailyTotalTokens: 1000
    dailyRouteTokens:
      mock: 50
- id: heart
  name: lykoi-heart
  config:
    # M2-W3 转正：基线/地板/显著性策略在插件内（env LYKOI_HEARTBEAT_BASELINE_MIN 可调基线）。
    checkIntervalMs: 1000
    stateFile: var/heart-state.json
    # salience_shadow.db 读侧（G-3）：dev 尚无 sidecar，留空=纯基线；env LYKOI_SALIENCE_DB 优先。
    salienceDb: ''
- id: llm
  name: '@deepseek-ai/dsh-llm'
- id: llm-mock
  name: lykoi-llm/mock
  config:
    provider: mock
- id: lykoi-llm
  name: lykoi-llm
# ==== M2 W5：对话心智（由 M1 converse-min 演进）+ telegram 器官 ====
# lykoi-converse = 真装配器 + 信封周期（S-23..S-53 + G-10）。它需要一个可写
# state 副本（dbPath）与 owner 域 persona TOML（personaToml）——两者都是治理侧
# 供给的部署事实，dev profile 默认 disabled；启用是治理侧动作（golden devstate
# 永远只读，不得直接接进来）。
- id: converse
  name: lykoi-converse
  disabled: true
  config:
    dbPath: var/dev-state.db
    personaToml: packages/lykoi-decide/test/fixtures/instance/persona.toml
    route: mock
    model: mock-model
    restartMarker: var/restart-marker.json
# telegram 生产传输默认 disabled（工单④）：M4-W1 起它接的是真 fetch，所以在 dev
# 装配面上「启用」这一位比从前更重的一步；启用是治理侧动作
# （需 lykoiMemory 的 state 副本路径与 tokenEnv 环境引用一并配好——凭据永不落明文）。
# dev 用 fake transport 的装配只存在于测试（lykoi-adapter-telegram/testing），不进 profile。
- id: memory
  name: lykoi-memory
  disabled: true
  config:
    dbPath: ../lykoi-cordis-devstate/memory.db
- id: telegram-transport
  name: lykoi-adapter-telegram/production
  disabled: true
  config:
    tokenEnv: LYKOI_TELEGRAM_BOT_TOKEN
    # 空串 = 直连（传输层零 env 读取，代理只能从装配面来；M4 前置 #8）。
    proxy: ''
- id: telegram
  name: lykoi-adapter-telegram
  disabled: true
  config:
    cursorPath: var/telegram-cursor.json
    archivePath: var/telegram-inbound.json
