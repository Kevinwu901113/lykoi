# 白皮书 v1.3 候选条目 与 9.4 R2 新体实证方案（2026-09-04）

- 性质：治理内部稿。候选条目在 Kevin 裁定前不具规范效力。
- 来源：`gpt_next_phase_memo_assessment_2026-09-04.md`（非规范评估稿，R-D）§三、§四、§六；Kevin 2026-09-04 裁定 R-A～R-D；WO-OUTCOME-01 复核。

## 1 · v1.3 候选条目（D3）

| # | 条目 | 触发来源 | 现状 | 依赖 |
|---|---|---|---|---|
| C-1 | 回合终局保证：每条 owner 入站恰有一个终局，技术失败以系统口吻回执，不由角色生成 | WO-OUTCOME-01；R-B | 代码已落（待合并） | 落地读数 24 h |
| C-2 | "语义沉默 ≠ 失败"从人格分层硬边界 §2.3 扩到基础设施失败 | 评估稿 §三第 7 行 | 与 C-1 同一事实基础 | C-1 |
| C-3 | 37.3 增补：传输层可按通道上限拆包，前提逐字；上限归通道（Telegram 4096），不进对话契约 | A4；GPT 修订 | 未实现 | WO-UTTER-01 |
| C-4 | 入站持久 spool：游标推进以落盘为准，重启回放合并 | A2 | 未实现 | WO-INGRESS-01 |
| C-5 | Message ≠ Turn ≠ Cognition ≠ Task 第三段（Task）：只在 D2 完成后写 | 评估稿 §三第 5 行 | 无 Task 对象 | D2（本稿 §2） |
| C-6 | Character ≠ Runtime ≠ Body 措辞：P-D1 / P-D3 / 37.4 合写一条；实例与身体一一绑定，不做同身体热切 | 评估稿第 9 条 c | 定案在，措辞分散 | Kevin 裁 P-D3 是否触发（R-A 已裁触发） |
| C-7 | `情绪脉冲`回路：契约声明的因果入口必须有消费者，否则不得写进契约 | 评估稿 §六.1 | 回路断 | 小单先补断点 |
| C-8 | 承诺终局：`promise_followup` 登记的跟进有自己的终局（completed / failed / expired），失败回执 | WO-CONTINUATION-01 | 待执行 | B3 落地 |
| C-9 | 实例事实审计口径：框架代码不得含实例事实（名字、对端、IP、用户名），出处唯一 = 实例包 | D4 审计稿；E4-SPEC | 审计完成，规范未写 | E4-SPEC |

处理方式：C-1、C-2、C-8 在各自单落地并有读数后合为一条"终局条款"提案；C-3、C-4 随 A2/A4 单；C-5 等 D2；C-6、C-9 可先写措辞稿给 Kevin。

**2026-09-05 进展**：措辞稿已写，见 `whitepaper_v1.3_wording_draft_2026-09-05.md`——含 C-3、C-4、C-6、C-7、C-9 五条可粘贴正文（8.7 / 8.8 新增，37.3 / 37.7 增补，37.4 / 36 章交叉引用），待 Kevin 逐条裁定。C-1 / C-2 / C-8 待落地读数，C-5 待 D2。

## 2 · 9.4 重评 R2 在新体上重做实证（D2）

R2 四前置（2026-08-22 稿，活体口径）：劈快照、推演/回流切分 + 推演零写入断言、memory.db WAL 评估、费用闸。新体（Cordis/TS）事实：

| 前置 | 新体现状 | 实证动作 |
|---|---|---|
| 1 劈快照 | wake 阶段 3/4 已是 `maintain(moment)` → `read(store, deps, m)` 两半共用一个时刻（SA-36） | 读 `lykoi-snapshot`：`read` 是否仍有写路径；写一条"read 零写"断言测试，跑通即前置 1 达成 |
| 2 推演零写入 | `wakeOnce` 注释称"G-9/SA-47 零写断言常驻本包测试" | 核对 `packages/lykoi-wake/test` 该断言是否覆盖 `buildCandidates` + `buildMessages` + LLM 调用整段；缺则补 |
| 3 WAL | `PRAGMA user_version` 两侧同为 0；journal 模式未核 | 服务器读 `PRAGMA journal_mode`；写只读推演与仲裁写互等的测量脚本（临时库） |
| 4 费用闸 | lykoi-budget 包存在；R-CA-1 记账点 = 一个 run_id 一次调用 | 核对 budget 是否有硬顶拒绝（不是只记账）；缺则 = 前置 4 未达 |

结论格式：四前置各一行"达成 / 未达 + 证据位置"。四件全达才解锁 37.5；任一未达，37.5 状态不变，Task Runtime（E2）不立项。预计体量：核对为主，一个复核周期；前置 3 需 Kevin 服务器一条只读命令。

## 3 · 已完成的 D1 改口

- `character_card_survey_2026-09-03.md` §7 结尾段：消息层"错字修正放发送器"与 37.3 相撞，改口为"发送器只拆包不改字；条数节奏与符号辞典属信封 utterances[] 的产生侧（A4）"。
- `persona_layering_design_v1_2026-09-01.md` §3.4：瞬时语气"装配时自然生效"在代码不成立（对话路径无 `getRegulation`），改口为待补断点。
