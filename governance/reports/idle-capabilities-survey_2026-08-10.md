# 闲置能力清点 · 建好了但没用上的东西 · 2026-08-10

数据源：Mac 本地备份副本（`20260809T032908Z`）的四个库，只查结构与用量统计，
不读记忆内容。缘起：Kevin 注意到 `permission_evidence` 表建着却没人喂，问"还有什么没用上"。

---

## 0. 先纠正一个我之前的误判

**感知链路是通的。** 934 个环境事件 → 注意力候选 934 → 决策 **865 attend / 69 decline**
→ 她的 4868 条 experiences 里 **1178 条来自 `environment`**。
（我先前看到 `CORE_ATTENTION_DECISION_CYCLE scanned=0` 以为管线空转，其实那是**追平后的
空闲心跳**，不是死。）

---

## 1. ⚠️ 她已经 55 天没有形成过一条新洞察

| 事实 | 数字 |
|---|---|
| 自主醒来次数 | **1844** |
| 产出的观察笔记（`autonomy_notes`） | 59（最新 **2026-08-08**） |
| 形成的洞察（`insights`） | **6**（最新 **2026-06-15**） |
| 经验总量（`experiences`） | 4868 |

6 条 insights 全部产于 6/4 与 6/15，类别是 `preference`/`persona`——看形态是早期种子，
**不是从 notes 晋升来的**。也就是说：她持续观察、持续记录，**但两个月没学到任何东西**。

这就是诊断信号 `CV1-LRN-001` 的实况：代码注释写着"晋升由 integrator 定期治理"，
而那段代码不存在，血缘表 `note_insight_links` 也是这次 P2-01 才建的（尚未有数据）。
白皮书说人格成长是 [PARTIAL]，物理原因在此。

**已在阶段 2 计划内**（设计 v1 §5 步骤 2：integrator 晋升作业），**现在是最该做的一件事**。

## 2. 显著性学习层：影子跑了 30 天，从未放行

- `salience_shadow.db`：`shadow_log` **1495 条**，时间跨度 **2026-07-10 → 2026-08-09**；
  `posterior` 10 个键，已学出有区分度的值（如 `wake_action:c8`=83.9、`conversation`=14.7、
  `silence`=5.0）。
- Phase 5 备忘录（2026-07-08 签发）的放行门：**影子期 ≥14 天且全局吸收率 ≥5%**，
  再过 Codex 放行门审计。**实际已影子 30 天**，条件早已到期，但**从未有人去评估放行**。
- 观察：`posterior` 的 beta 全是 1.0（先验未动）——只累积正证据，无负证据。
  Reward 锚是"仅 nightly integration 的 accepted_op 物理引用"，而 nightly 晋升本身是坏的
  （见 §1）——**两个缺陷互相锁死**：晋升不工作 → 没有 accepted_op → bandit 学不到负例。

**建议**：修完 §1 的晋升作业后，重新评估显著性放行——这两件事是同一个结的两端。

## 3. 权限学习：表建好了，几乎没喂

`permission_decision_facts` 只有 **3 条**，全部产生于 **2026-08-01 一天**，
全是 `terminal.exec` 的 `approved`，scope 字段值 `episodic`。

两个含义：
1. **这条链基本没通**——Kevin 在 app 里批准/拒绝过的次数远多于 3。
2. **但 schema 早已预留了 Kevin 要的东西**：`scope='episodic'` 意味着"仅此一次"，
   与之相对的常设授权（`approval_model_v1` 的 ask-once）**字段位置已经在那儿了**。

**建议**：S2 审批解释器实现时直接接上这张表，`scope` 用来区分"这一次"与"以后都行"。

## 4. 执行评估：250 条结果，从未被评估过

`core_facts.db` 的 `outcomes` 250 条**全部**是 `evaluation_kind='unassessed_legacy'`
（被 CHECK 约束钉死）。她执行过 250 次命令、留了 250 条观察与结果，
**但没有任何一条被判断过"做得好不好"**。

这正是 `procedures.reliability`（P2-01 刚建）唯一合法的数据源。
**建议**：与设计 v1 §5 步骤 4（shadow 解钉）合并处理。

## 5. 建了从没用过的两张表

- `core_facts.episodes`：**0 行**
- `memory.owner_edits`：**0 行**——Kevin 从未用过所有者编辑机制修改她的人格/记忆

（不一定是问题，但值得知道：要么用起来，要么承认它们是死设计。）

## 6. 运维盲区：她有过两段"失语期"，没人收到告警

| 时段 | 症状 | 次数 |
|---|---|---|
| 2026-07-25 ~ 07-26 | `400 Bad Request`（api.deepseek.com） | 64 次醒来 |
| 2026-07-29 ~ 07-31 | **`402 Payment Required`**（欠费） | 80 次醒来 |

期间她按时醒来，但**无法思考**——每次醒来只拿到一个 API 错误。8/01 起恢复正常，
现在健康。**问题不在于故障本身，而在于没有任何告警**：她"哑了"三天，
是我今天翻数据库才发现的。

**建议**：加一条轻量健康告警（连续 N 次自主循环返回 LLM 错误 → 通知 Kevin）。
成本极低，属于"她的身体自感知"（需求分析 D5）的最小落地。

---

## 优先级建议

| 序 | 事项 | 理由 |
|---|---|---|
| 1 | **integrator 晋升作业**（§1） | 她 55 天没学到东西；且是 §2 与 §4 的前置结 |
| 2 | **LLM 失败告警**（§6） | 半天工作量，防止再哑三天没人知道 |
| 3 | **权限学习接线**（§3） | 与 Kevin 刚定的 ask-once 审批模型同一件事，顺手做 |
| 4 | 显著性放行评估（§2） | 待 §1 修好后重新起算 |
| 5 | 执行评估解钉（§4） | 依赖收据设计，排在 Gateway 之后 |
