# WO-TELEGRAM-MULTIPART-01

Continue authorized Telegram attachment delivery. Live export got HTTP400 despite correct destination and approval. Reproduction: native FormData passed to external undici8 fetch produces text/plain body [object FormData]; native fetch direct path worked. Convert FormData at the existing proxy HTTP boundary into that client's FormData class, preserving fields, filename and Blob bytes. No permission, retry, dependency, schema or production config changes. Test the real external serializer over loopback; owner deploy then fresh approval/attachment acceptance.
