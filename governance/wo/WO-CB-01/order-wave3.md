# WO-CB-01 · 续跑单(第 3 波) · 直接落盘,基线复跑豁免

**为什么又是续跑单**:第 2 波(03:11-03:24)重蹈第 1 波覆辙——在基线 chunk 3
跑动中"挂了监听"然后交卷退出(headless 会话退出即杀掉你的一切子进程,
"监听"不存在)。侦查与暂存物两波都健在,**不重做**。

**裁决:改动前基线复跑豁免**。基 `32238013` 的全量基线已由 GW-01 复核在
同一 commit 上权威确立 = **2169/3/6**(collect 2178,治理侧另有独立 collect
对账)。两波续跑都死在"先复跑基线"这 2 小时上,收益为零——豁免之,直接动手。
你的**改动后**全量直接与 2169/3/6 对账;`test_core_v1_shadow` 若有浮动按
教训 38 定性(同机另有执行器)。第 1-2 波的 `baseline/chunk_{1,2,3}.log`
作为旁证引用即可。

**结束纪律(两波败因,零容忍)**:最终输出只能是完成全部判据后的完整报告。
每个 pytest 块**前台跑完**才继续;**禁止后台、禁止挂监听、禁止"等会儿接上"**;
中途状态写进 commit message,不写进 stdout。

你仍在 `~/lykoi-work-cb` 分支 `wo/cb-heart`(基 32238013,树应干净)。原工单
= 本目录 `order-wave1.md`,判据①-⑪与 forbidden 原样有效;暂存物在
`~/wo/WO-CB-01/stash/`(heartbeat.py + 6 个 test_cb_*.py,是草稿不是判据,
落树时逐条对齐原判据补缺)。`eq_before.json`(改动前等价哈希)与
`log_rate.json` 直接引用。铁律:每判据一 commit `[WO-CB-01]`、timeout 1800
包裹(可分块)、stdout 即报告本体、冲突停下写清楚。

## 本波任务(按序)

A. 落盘实现,按原判据逐 commit:步 0 劈快照(②,"后"侧等价哈希对
   `eq_before.json`)→ 零写入断言(③,先红后绿写进 commit 说明)→ 骨架补测
   (④)→ R-CA-1 修复(⑤)→ 心跳影子件+接线(⑥)→ salience 输入(⑦)→
   llm_call 自主侧归因(⑧)。
B. 零扰动自证(⑨)+ 改动后全量(⑩,分块前台串行,对账 2169/3/6+新增;
   新增失败零容忍逐条解释;manifest 重签前后条数写明,基 112;conftest
   教训 36)。
C. 报告(⑪,stdout 本体):侦查节(两波产物并入)、每判据自证、影子读数
   使用说明(查询口径)、步 3/步 4 交接清单。
