# WO-CB-01 · 续跑单(第 2 波) · 落盘实现 + 全量 + 报告

**为什么是续跑单**:第 1 波(opus,2026-08-22 00:00-01:15)完成了侦查、manifest
独立重算(112/0 差异)、等价性"改动前"哈希(`eq_before.json`)、日志速率实测
(`log_rate.json`)、6 个测试文件与 `heartbeat.py` 的起草(暂存 `~/wo/WO-CB-01/
stash/`)、基线分块 1-2(354p1s / 282p)——**但在基线 chunk 3 进行中把"还在跑"
当最终输出交卷,会话随即结束**。树内零 commit,判据②⑤⑥⑦⑧的树内改动一个
未落。侦查结论与暂存物**不重做、要复用**。

**结束纪律(第 1 波败因,本波铁律)**:你的最终输出只能是完整报告。任何长任务
(基线、全量、单个测试块)没跑完,就前台等它跑完;**绝不允许以"还在跑、稍后
接上"收尾**。中途不写 report 式总结——把状态留在 commit message 里。

你是执行 Agent,在 `~/lykoi-work-cb` 工作,分支 `wo/cb-heart`(基 `32238013`)
已 checkout,工作树应干净(若有残留先核对再处置)。原工单全文 = 本目录
`order.md`,其判据①-⑪与 forbidden **原样有效**;参考材料同目录。铁律:前台
串行、禁后台、每判据一 commit(`[WO-CB-01]` 前缀)、测试 `timeout 1800` 包裹
(允许沿用你的 `run_suite.sh` 分块形态——每块独立 timeout 1800,前台串行)、
stdout 即报告本体、冲突停下写清楚。**并行注意(教训 38)**:同机 WO-GW-02
执行器在跑,CPU 争用照旧,core_v1 段 TimeoutError/连锁 F 按教训 38 处置。

## Do-not-redo(复用,不重做)

| 产物 | 位置 | 用法 |
|---|---|---|
| 侦查结论 | 第 1 波报告(本目录 `report.attempt1.md` 已被覆盖为你的新报告——侦查节按原判据①重建入最终报告,可借暂存物加速) | 判据①仍须完整成节 |
| 改动前等价哈希 | `eq_before.json` | 判据②的"前"侧证据,直接引用 |
| 日志速率实测 | `log_rate.json`(7.33 条/拍) | 判据①d/⑥的口径证据 |
| manifest 独立重算 | 112/0 差异 | 报告引用,判据⑩重签仍要做 |
| 基线 chunk 1-2 | `baseline/chunk_{1,2}.log` | 可引用;chunk 3-6 补齐后合成基线总数 |
| 实现草稿 | `stash/heartbeat.py` + 6 个 `test_cb_*.py` | 落盘起点,按判据逐一 commit(清 `__pycache__`) |

## 本波任务(按序)

A. **补齐改动前基线**:用 `run_suite.sh` 跑完 chunk 3-6(chunk 1-2 引用存档),
   合成总数与 2169/3/6 对账;core_v1 段失败按教训 38 单独串行复跑定性。
   **基线没对上账之前不落任何树内改动**;对不上停下写清楚。
B. **落盘实现,按原判据逐 commit**:步 0 劈快照(判据②,等价性用
   `eq_before.json` 对照"后"侧)→ 零写入断言(③,先红后绿入 commit 说明)→
   骨架补测(④)→ R-CA-1 修复(⑤)→ 心跳影子件+接线(⑥)→ salience 输入(⑦)→
   llm_call 归因(⑧)。stash 文件落树时逐一核对与原判据的每条要求对齐,
   缺的补上——stash 是草稿不是判据本身。
C. **零扰动自证(⑨)+ 全量(⑩)**:改动后全量(分块可),新增失败零容忍逐条
   解释;manifest 重签前后条数写明(基 112);conftest 新增常量核对(教训 36)。
D. **报告(⑪,stdout 本体)**:原判据⑪全部要求——①全节、每判据自证、影子
   读数使用说明(查询口径)、步 3/步 4 交接清单。
