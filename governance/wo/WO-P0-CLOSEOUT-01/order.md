# WO-P0-CLOSEOUT-01

基线 main@9beca13；分支 wo/p0-closeout-01。

P0 目标是减负：删除旧代码、不合适逻辑、重复兜底和迁移时期的实现约束，保留真实信任边界、数据一致性及不可逆副作用防御。

本批范围：剩余 JSON recovery、学习失败如实上报、损坏账本不再假装正常、历史注释及等价性测试退休。用户补充后已撤出 kernel 实例化、出站服务搬迁及新增契约工程；这些属于后续架构工作。本批不新增 Runtime 服务，不部署生产。

验收：针对行为变化做故障注入；保留现有审批、持久化、真实 HTTP 传输与回合测试；全量测试、typecheck 和 CI。清理注释以编译后代码不变校验。机器输出和一次性脚本不入 Git。
