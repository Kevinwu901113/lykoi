# WO-SUBTRACTION-01 — 全仓减法审计与等价简化

授权：Kevin 于 2026-09-05 要求检查此前 GPT-5.6 Sol / Claude 协作中的过度防御、重复实现与复杂性，并对整个项目做减法；执行指定 GPT-5.6 Luna / max，主 Agent 独立复核。

状态：已实施并通过回归，交付见同目录 review.md；尚未合并/部署。

基线：GitHub `Kevinwu901113/lykoi` HEAD 与本地 main 同为 `c03ffffe5d55161173fb220fdba870791e2acc9c`。工作副本 `/Users/wukevin/lykoi/subtraction-audit`，分支 `wo/subtraction-01`。

范围：17 个运行时包与 profile 工作区、profile、测试/依赖、当前协作入口与历史规则。先全仓扫描，再按真实调用、历史修复原因和测试筛选语义保持的修改。不能凭代码长或 catch 多判定过度防御，也不能从 git 作者名推断实际模型。

实施：三路执行 Agent 先只读提交候选；主 Agent 确认具体改动后实施。不得新增通用框架为复用而复用。不得改变人格/提示词语义、持久化格式、投递与审计语义、审批权限或预算策略。kernel/gate 仅在本单明确范围与复核下修改。保留真实外部边界校验、故障恢复和历史数据兼容。

交付：可复核 diff、全仓发现与保留理由、基线/改后全量测试和 typecheck、分支提交。修改不涉及生产、main 合并或活数据。合并与部署遵循已有所有者决定。

验收：每项简化能解释减少哪一份重复/哪一个无必要分支；已有测试零新增失败；针对真实行为风险补最少必要测试；完整性清单由生产部署独立重签，不伪造开发机生产 gate 通过。
