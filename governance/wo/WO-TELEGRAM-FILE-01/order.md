# WO-TELEGRAM-FILE-01

Owner accepted completion of workspace-file → Telegram attachment delivery after live approval acceptance exposed text-only outbound. Base c5a84d4. Existing merge authorization continues; owner performs root deployment.

Add a distinct messenger.send_file action with workspace-relative path, context_id and optional reply_to. Kernel hard approval on every export; no text-grant expansion, autonomous permission, or exemption. Current-instance bounded regular-file bytes only. Telegram sendDocument multipart uses existing proxy/timeout/error sanitization. Require message id for success; ambiguous network results are not automatically replayed. No dependency, schema, profile or Gate changes.

Validation must include file boundary failures, denied/unapproved zero network, real multipart bytes over loopback and success/failure receipts, production bridge, full tests/typecheck, and independent privileged-code review. Live Telegram file download remains owner-deploy acceptance.

Direct conversations use the selected runtime instance stateRoot/workspace, matching instance assembly; Task operations use their trusted execution workspace. Approval authorizes sending the current bytes at the named path when executed; it does not claim an immutable content snapshot from question time. Files are bounded snapshots once read, never reopened for HTTP.

Protocol reference: https://core.telegram.org/bots/api#senddocument (multipart document upload).
