# Capability context repair

## Live evidence

On f579b18, a fresh reminder was created, paused, naturally approved for resume, and delivered verbatim once in Telegram. Stored dueAt remained unchanged across pause/resume; delivery state sent, attempts1. This sample passes.

A fresh owner coding request failed: direct Pi had no Task execution context; subsequent task.create recorded autonomous origin, whose Pi dispatch was denied by existing policy. The foreground then proposed another direct Pi operation despite naming the Task only inside prompt text. The Task reached failed with an empty workspace; no program success is claimed. A later cancellation command reported already terminal; it was not falsely recorded as successful cancellation.

## Change

Capability declarations can specify availableIn conversation/wake/task. Runtime snapshots and filters immutable metadata for discovery; Conversation rejects unavailable external calls before approval, after existing unknown/unwired diagnostics. Wake and Task use their respective discovery contexts. task.create remains autonomous and is only offered to Wake; Pi dispatch is offered only to Task. Owner background jobs continue through conversation.promise_followup. Pi approval shows bounded actual prompt text.

This metadata is a discovery/context contract, not a universal Runtime authorization check. Existing Kernel permissions, Runner execution binding and user/autonomous Task origin rules remain authoritative. No permission broadening, state reset, dependency, schema, profile or cap changes.

Independent governance review ACCEPT. Typecheck passed; full suite1284/pass1273/fail0/skip11. Initial context check placement intercepted existing unknown/unwired diagnostics; moving it after buildAction preserved those diagnostics, and all regressions passed. New owner Task -> approved Pi -> artifact acceptance remains pending deployment. Exact production IDs and raw evidence remain in the local landing record.
