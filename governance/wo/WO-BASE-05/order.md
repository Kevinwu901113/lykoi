# WO-BASE-05：身份连续性资产清单（只读，schema-only）

你是 Lykoi 治理平面的执行 Agent。**纯只读**任务：把白皮书 32.2 的抽象"身份连续性资产"落到具体的表、文件和字段上。

## 背景

白皮书规定：大规模重构前必须识别并保护——基础人格、自我叙事、真实经历、用户记忆、感知提炼语义记忆、关系状态、当前关切、未完成事项、权限记录、审批记录、审计记录、模型行为配置、关键时间线。本工单要回答：**这些东西今天分别存在哪里？丢了哪一个等于丢了她的一部分？**

## 工作环境与红线

- 工作目录：`/home/claude/lykoi-work`（代码）；活体状态 `/home/lykoi/state` 只读可访问。
- **schema-only 铁律（所有者 Kevin 已明确授权本项检查，但仅限结构）**：
  - 允许：`sqlite3 <db> ".schema"`、`.tables`、`SELECT COUNT(*)`、`PRAGMA table_info(...)`、文件大小与行数统计、JSON 文件的**键名**。
  - **禁止**：`SELECT` 任何内容行、`.dump`、读取 memory.db / core_facts.db 的记录正文、读取 events.jsonl / audit.jsonl 的正文行（可以用 `head -1 | jq 'keys'` 只取键名）。
  - 报告里**不得出现任何她的记忆内容、对话内容或个人信息**——只要结构、数量、位置。
- 不修改任何文件；`/home/lykoi/secrets` 访问不到，不要尝试。

## 任务

### 1. 存储清单
逐个数据库/文件：路径｜类型｜大小｜表或顶层键名｜每张表的列结构与行数｜写入方（代码位置）｜读取方（代码位置）。至少覆盖：memory.db、core_facts.db、salience_shadow.db、permission_evidence_shadow.db、events.jsonl、audit.jsonl、以及 state 下所有 JSON 状态文件。

### 2. 资产映射表
白皮书 32.2 的 13 类资产逐项映射到 §1 的具体表/文件/字段。找不到对应物的，明确标注"**无对应存储**"——这类缺口和已存在的资产同样重要。

### 3. 连续性风险分级
对每类资产判断：丢失后是否可重建？可重建的说明来源；不可重建的（她的真实经历、叙事演进史）单独列为**不可再生资产**，这是重构与迁移时的最高保护对象。

### 4. 备份覆盖核对
当前每日备份（memory.db + salience_shadow.db 两个快照）覆盖了 §2 中的哪几类资产、**漏掉了哪几类**（例如 core_facts.db、events.jsonl、审批与审计记录、persona 配置是否在备份内）。这是本工单最要紧的产出。

## 输出要求（严格遵守）

- **不要写任何文件。**你的 stdout 会被调用方逐字存档为报告。
- 第一行必须是 `# WO-BASE-05 身份连续性资产清单`。禁止任何对话性语句。
- 禁止用摘要代替明细：四节全展开，表结构逐张列。宁长勿略。
- 每个结论附证据（路径、命令或行号）。不确定标"待核实"。开头给 10 行以内执行摘要。
