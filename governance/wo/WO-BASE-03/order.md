# WO-BASE-03：白皮书风险清单逐条核实（只读）

你是 Lykoi 治理平面的执行 Agent。**纯只读**任务：把白皮书第 30 章的风险断言与实际代码逐条对账，证实或证伪都算结论。

## 工作环境

- 工作目录：`/home/claude/lykoi-work`（治理平面工作副本，与活体检出内容一致）
- 活体状态目录 `/home/lykoi/state` 你有**只读**权限，可用于核实文件是否存在、大小、权限；**不得读取 memory.db / core_facts.db 的内容行**，也不得读取任何 `.jsonl` 的正文内容（只看行数、大小、字段名）。
- `/home/lykoi/secrets` 你访问不到，不要尝试。

## 硬约束

- 不修改任何文件、不切分支、不运行有副作用的程序、不访问网络。

## 逐条核实清单

对每一条给出：**结论（证实/证伪/部分属实）+ 代码或文件证据（路径:行号）+ 影响面**。

1. **Secret 仍以明文环境文件为主**：找出所有读取凭证的路径，确认是否存在 Vault/句柄机制。只列变量名与读取位置，不读值。
2. **memory/ 未完全纳入完整性清单**：找到完整性清单（integrity manifest）机制的实现与其覆盖范围，列出**未被覆盖**的关键路径。
3. **某些有界队列可能静默逐出数据**：找出所有有界队列/环形缓冲/截断逻辑（maxlen、trim、tail -n、LIMIT 等），逐个判断逐出时是否有日志或告警，列出静默丢弃的位置。
4. **Insight 运行时写入链不完整**：追踪 persona insight 从产生到持久化的完整调用链，指出断点在哪一环。
5. **Owner Edit 记录结构存在但缺少生产调用者**：找到该结构定义，用 grep 交叉验证是否只有测试在调用。
6. **滚动摘要未形成完整持久化连续性机制**：找到滚动摘要实现，说明它在重启/模型切换后是否存活。
7. **自主进程的工作目录**：确认自主行动（浏览器、终端等）执行时的 CWD 是什么。背景：曾在核心仓库根发现自主探索产生的杂散文件（一个 HTML 存档、一个 curl cookie 罐）。给出 CWD 的代码证据，并说明是否存在把产物写进代码仓库的路径。

## 输出要求（严格遵守）

- **不要写任何文件。**你的 stdout 会被调用方逐字存档为报告。
- 第一行必须是 `# WO-BASE-03 风险清单逐条核实`。禁止任何对话性语句（"我已完成""需要继续吗""请授权"等）。
- 禁止用摘要代替明细：7 条逐条展开，一条都不能少。宁长勿略。
- 每条附证据（路径:行号）。不确定标"待核实"，不要猜测。开头给 10 行以内执行摘要。
