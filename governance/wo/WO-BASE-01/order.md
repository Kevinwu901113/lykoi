# WO-BASE-01：核心仓库代码资产清点（只读）

你是 Lykoi 治理平面的执行 Agent，受本工单驱动。本工单为**纯只读分析**任务。

## 目标
摸清 `/home/lykoi/projects/lykoi` 代码资产全貌，为白皮书第 31 章基线审查提供代码资产清单。

## 硬约束（禁区）
- 不修改、创建、删除任何文件；不执行任何写命令；不切换分支；不运行测试或任何会产生副作用的程序
- 不访问 `~/state`、`~/secrets`、`~/runtime/core-v1/core.sock`
- 不访问网络
- 配置分析只列变量名和读取位置，**不读取任何密钥值**

## 任务（按此结构输出报告）

### 1. 顶层模块清单
仓库内每个 Python 包/顶层目录一行：名称｜用途（读代码判断，一句话）｜规模（文件数、总行数）｜关键入口点。

### 2. 四进程映射
guardian/watchdog.py、lykoi.surface.app（uvicorn）、lykoi.core.runtime、lykoi.cognition.autonomous 四个运行进程各自的启动链和核心依赖模块（模块名列表即可）。

### 3. 死代码与废弃候选
- 无任何引用的模块/文件（用 grep 交叉验证后再列，标注验证方式）
- 默认关闭且长期未启用迹象的 feature flag
- TODO/FIXME/XXX 密度异常的文件（列前 10）

### 4. 测试资产
测试文件数与总行数；按模块统计哪些核心模块有测试覆盖、哪些明显没有（不用跑测试，看文件对应关系即可）。

### 5. 配置面
所有读取环境变量或配置文件的位置：变量名｜读取文件:行号｜用途推断。只列名字，不读值。

### 6. 文档与代码冲突
仓库内 docs/、README、设计文档声称的行为与代码明显不符处（每条给文档位置和代码位置证据）。

## 输出要求（严格遵守）
- **你的 stdout 会被逐字存档为 report.md——它就是报告本体，不是给人看的聊天回复。**
- 第一行必须是 `# WO-BASE-01 代码资产清点报告`，之后直接展开六个任务的完整明细。
- 禁止输出任何对话性语句（"已完成""报告已生成""需要继续吗"等）；禁止用摘要代替明细；宁长勿略——模块清单、变量清单、文件清单要**逐项列全**。
- 每个结论附证据（文件路径、行号或统计命令）。
- 不确定的地方标注"待核实"，不要猜测。
- 报告开头给一段 10 行以内的执行摘要，然后是全部明细。
