# WO-U3S · 续跑单(第 2 波) · 收尾:manifest + 全量 + 报告

**为什么是续跑单**:第 1 波(opus,2026-08-21 20:18 CST 起)五个判据 commit 已全部
落分支,随后派发器进程组死亡(run.log 无 EXIT 行、报告 0 字节)——非工单问题、
非你的问题。实质工作**禁止重做**;你的任务 = 处置未提交残留、存量自证、跑全量、
出完整报告。

你是执行 Agent,在 `~/lykoi-work-l1` 工作,分支 `wo/u3s`(基 `7b00ae5e`)已
checkout。铁律不变:前台串行、**禁后台**、每判据一 commit(`[WO-U3S]` 前缀)、
测试 `timeout 1800` 包裹、**stdout 即报告本体**、宁长勿略;侦查发现与工单或
存量 commit 冲突时,停下写清楚。原工单全文见本目录 `order-wave1.md`,其
forbidden 原样继承。

## Do-not-redo 表(已在分支,只验不重做)

| 判据 | commit | 内容 |
|---|---|---|
| ①②③⑥ | a20ad743 | 信封周期接管 inbound 轮——转录机让位、影子停跑、念头转正 |
| ⑤ | 2dd935bc | 红测试交接——零个读者退役,恰好一个读者接班 |
| ⑧ | 8ab9bb76 | 审批问句送达在切换态承重 |
| ④ | ace0824c | 零扰动——开关关闭态一个字节不动 |
| ⑥ | f4cf30cc | P1/P2 在主路径承重——E1/E2 全套开关开启态复跑 |

**未提交残留**:`guardian/manifest.sha256` 有一处未提交修改(第 1 波中断时的
半截重签)。

## 本波任务(按序)

A. **manifest 处置(判据⑦前半)**:不信残留——按 guardian 既有重签流程对工作树
   现状**全量重算**,核对与基分支(`git show 7b00ae5e:guardian/manifest.sha256`)
   的差异**仅涉本单触达的文件**;前后条数写明(以基分支现值为基准,不要凭记忆
   写数字)。单独 commit `[WO-U3S] 判据⑦: manifest 重签`。
B. **存量校验(只验不改)**:逐条跑五个存量 commit 各自的新增/关键套件,自证
   行为仍成立(①-⑥⑧每条给证据:测试名单+数字或代码行引用)。发现缺陷才改,
   改必单独 commit 并标注"第 2 波修正"。
C. **判据⑦全量**:全邻接前台串行,基线 **2108/3/6**(基 `7b00ae5e`);先复跑
   核实基线再对账;新增失败零容忍逐条解释。**并行执行注意(教训 38)原样有效**:
   同机可能另有执行器在跑,若 `test_core_v1_shadow` 出现 TimeoutError 连锁
   失败,记录后对该文件单独串行复跑一次定性,勿逐条归因。
D. **报告(stdout 本体)**:每判据自证(含存量五条的校验证据);部署核对信息
   (原判据⑦要求:进程/单元/env——预期仅需 lykoi-server drop-in 加
   `LYKOI_U3_SWITCH_ENABLED=1`,代码合并与开关开启是两个独立动作,合并包
   写两段验证);原判据①③⑧的行为说明引用代码行;conftest 默认表核对
   (开关在测试默认关)。
